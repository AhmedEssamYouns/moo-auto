name: Deploy Moo Automotive React to Registry

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Debug - List files in the workspace
        run: ls -la

      - name: Log in to private Docker registry
        run: |
          echo "Admin123" | docker login registry.sevencsolutions.com -u "jimmy" --password-stdin

      - name: Build the Docker image
        run: |
          if [ ! -f Dockerfile ]; then
            echo "❌ Dockerfile not found!"
            exit 1
          fi
          docker build -t registry.sevencsolutions.com/moo-react:latest -f ./Dockerfile .

      - name: Push the Docker image
        run: docker push registry.sevencsolutions.com/moo-react:latest
